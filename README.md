# Weather_Dashboard
A simple program that shows the current weather for a city and saves the results to a JSON file.

## Concepts Covered

- HTTP requests with `requests`
- JSON parsing (`json` module)
- File reading & writing
- Loops, functions, error handling
